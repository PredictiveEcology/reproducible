% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache-repo.R
\name{mergeShownCacheCloud}
\alias{mergeShownCacheCloud}
\title{Fold cloud cache metadata into a local \code{showCache} result for \code{showSimilar}}
\usage{
mergeShownCacheCloud(shownCache, cloudShown, .functionName = NULL)
}
\arguments{
\item{shownCache}{The local \code{\link[=showCache]{showCache()}} \code{data.table}.}

\item{cloudShown}{The cloud metadata \code{data.table} from \code{\link[=showCacheCloud]{showCacheCloud()}}.}

\item{.functionName}{The function name to keep from \code{cloudShown} (or \code{NULL} for all).}
}
\value{
A combined cache \code{data.table}.
}
\description{
Restricts the cloud-sourced metadata (from \code{\link[=showCacheCloud]{showCacheCloud()}}) to the same
function as the current call, then \code{rbind}s it onto the local \code{shownCache},
de-duplicating on \code{cacheId}/\code{tagKey}/\code{tagValue}. Pure (no I/O) so the merge
behaviour can be tested without Google Drive.
}
\keyword{internal}
