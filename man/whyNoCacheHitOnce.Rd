% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/whyNoCacheHit.R
\name{whyNoCacheHitOnce}
\alias{whyNoCacheHitOnce}
\title{Report on the next cache miss that looks accidental, once}
\usage{
whyNoCacheHitOnce(k = TRUE, verbose = getOption("reproducible.verbose"))
}
\arguments{
\item{k}{\code{TRUE} to fire at the first such miss (the default), or a number:
fire only when the closest earlier call differs in at most \code{k} elements, so
a genuinely different job does not trip it. \code{FALSE} cancels an arming.}

\item{verbose}{Numeric or logical; controls messaging.}
}
\value{
Invisibly, \code{TRUE} when armed, \code{FALSE} when cancelled.
}
\description{
The cache equivalent of \code{\link[=debugonce]{debugonce()}}. Arm it, run the thing that recomputed
when you expected it not to, and the first call that fails to reuse the cache
-- \emph{and} has an earlier call of the same function to compare against --
prints which elements differ and stops. The arming is then spent, so the next
run is unaffected: nothing to remember to switch off.

This is the form to reach for in a pipeline, where the \code{Cache()} calls belong
to modules rather than to you, so there is no call of yours to put
\code{dryRun} on and no \code{cacheId} to hand to \code{\link[=whyNoCacheHit]{whyNoCacheHit()}} afterwards.

A miss with nothing to compare against is new work, not a slip, and does not
spend the arming -- it waits for a miss that looks accidental.
}
\examples{
\dontrun{
whyNoCacheHitOnce()      # arm
mySimulation()           # stops at the first accidental miss, naming the element
}
}
\seealso{
\code{\link[=whyNoCacheHit]{whyNoCacheHit()}}, which explains an entry or a whole run after the
fact, and \code{options(reproducible.stopOnCacheMiss = )}, the sticky form of
this switch.
}
