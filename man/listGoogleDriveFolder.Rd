% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{listGoogleDriveFolder}
\alias{listGoogleDriveFolder}
\title{List a Google Drive folder, using a mirror listing when remapped}
\usage{
listGoogleDriveFolder(
  url,
  pattern = NULL,
  verbose = getOption("reproducible.verbose", 1)
)
}
\arguments{
\item{url}{A Google Drive folder URL or bare id.}

\item{pattern}{Optional regular expression; only files whose \code{name} matches are
kept (applied to both the mirror and Drive paths).}

\item{verbose}{Numeric verbosity level.}
}
\value{
A \code{data.table} with columns \code{name} (file name), \code{id} (the Google Drive
file id when listed from Drive; \code{NA} on the mirror path), and \code{url} -- a URL
to hand to \code{\link[=prepInputs]{prepInputs()}}: the public mirror URL when remapped, otherwise the
Drive file URL. A 0-row table when the folder is empty/unreachable.
}
\description{
A remap-aware alternative to \code{googledrive::drive_ls()} for \emph{listing} a Google
Drive folder's files. When the folder id is in the \code{reproducible.urlRemap}
manifest as a \emph{directory remap} (a \code{type = "dir"} row; see \code{\link[=makeUrlRemap]{makeUrlRemap()}}),
the files are enumerated from the public bucket listing with \strong{no Google
authentication}. Otherwise it falls back to \code{googledrive::drive_ls()} (which
authenticates as usual). It is meant as a near drop-in for the common
\code{as.data.table(drive_ls(url))} pattern: filter the returned \code{name}s, then pass
the corresponding \code{url}s to \code{\link[=prepInputs]{prepInputs()}}.
}
\examples{
# A directory-remap manifest maps a Drive *folder* id to a bucket prefix-listing
# URL; build it with buckethost::makeMirrorManifest(directories = TRUE), or by
# hand (a `type = "dir"` row):
manifest <- data.frame(
  filename = "2020",
  url = paste0("https://object-arbutus.cloud.computecanada.ca/",
               "predictiveecology/?prefix=SCANFI_v2/2020/&delimiter=/"),
  id = "15T4HIFeqzwp0TuOuxmYoexuXdLFnCZBi",
  type = "dir"
)
remap <- makeUrlRemap(manifest)  # the "dir" row populates the folder-listing map

# Then, with `options(reproducible.urlRemap = manifest)` set, a call such as
#   files <- listGoogleDriveFolder(
#     "https://drive.google.com/drive/folders/15T4HIFeqzwp0TuOuxmYoexuXdLFnCZBi")
# lists that folder from the public mirror with no drive_auth(), returning
# name/id/url columns to feed to prepInputs(); without a manifest it falls back
# to googledrive::drive_ls() (which authenticates as usual).
}
\seealso{
\code{\link[=makeUrlRemap]{makeUrlRemap()}} for the directory-remap manifest;
\code{buckethost::makeMirrorManifest(directories = TRUE)} to build one.
}
