% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{.dlGooglePublic}
\alias{.dlGooglePublic}
\title{Download a \emph{public} Google Drive file without authentication}
\usage{
.dlGooglePublic(
  url,
  destinationPath,
  targetFile,
  verbose = getOption("reproducible.verbose", 1)
)
}
\arguments{
\item{url}{The original Google Drive URL (any form).}

\item{destinationPath}{Directory to download into.}

\item{targetFile}{The resolved Drive filename (so the on-disk name and
checksum bookkeeping match the authenticated path).}

\item{verbose}{Numeric verbosity level.}
}
\value{
A list with \code{destFile}.
}
\description{
Internal. Used by \code{\link[=dlGoogle]{dlGoogle()}} when the user supplies the public web-download
form of a Drive URL (or sets \code{reproducible.gdriveNoAuth = TRUE}). It fetches
the file over plain HTTPS via \code{\link[=dlGeneric]{dlGeneric()}} -- no googledrive token required.
}
\details{
Small files stream directly. Large files (>~100 MB, or any that trip Drive's
"can't scan for viruses" check) first return an HTML interstitial carrying a
one-time \code{confirm} token and \code{uuid}; these are parsed out and the request is
resubmitted to \code{drive.usercontent.google.com/download}. If the second attempt
still returns HTML, the file is almost certainly not shared "Anyone with the
link" and we error rather than silently save the page.
}
\keyword{internal}
