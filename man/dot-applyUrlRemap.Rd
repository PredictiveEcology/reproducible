% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{.applyUrlRemap}
\alias{.applyUrlRemap}
\title{Apply the user-supplied URL remap hook}
\usage{
.applyUrlRemap(url, filename, verbose = getOption("reproducible.verbose", 1))
}
\arguments{
\item{url}{The original (resolved) URL.}

\item{filename}{The resolved target filename, passed to the hook so the user
can remap based on filename, the original url, or both.}

\item{verbose}{Numeric verbosity level.}
}
\value{
A length-one character URL to download from (possibly the original).
}
\description{
Internal. Consults \code{getOption("reproducible.urlRemap")}. When that option is a
function, it is called as \code{fn(url, filename)} once the target \code{filename} has
been resolved (which, for Google Drive URLs, requires the \code{drive_get()} lookup
in \code{assessGoogle()}). The function may return a replacement URL to download
from instead. A \code{NULL} return, a non-character or empty return, a return
identical to the original \code{url}, or \emph{any error} (which emits a \code{warning()}
rather than failing the download) all mean "keep the original url". This way a
broken remap can never break a download.
}
\keyword{internal}
