% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{.remapUrlEarly}
\alias{.remapUrlEarly}
\title{Resolve and remap a URL early, before the COG fast-path}
\usage{
.remapUrlEarly(url, verbose = getOption("reproducible.verbose", 1), ...)
}
\arguments{
\item{url}{The original URL (a Google Drive URL, a bare Drive ID, or HTTP(S)).}

\item{verbose}{Numeric verbosity level.}

\item{...}{May carry \code{team_drive}/\code{shared_drive} for the Drive lookup.}
}
\value{
A length-one character URL (possibly the original).
}
\description{
Internal. Called near the top of \code{\link[=prepInputs]{prepInputs()}} so that a Google Drive URL can
be redirected to a Range-capable mirror \emph{before} the COG fast-path decision.
\code{\link[=prepInputsCOG]{prepInputsCOG()}} only triggers for \verb{https://....tif}-style URLs, so a Drive
URL or bare Drive ID would otherwise never benefit from partial \verb{/vsicurl/}
reads. When \code{reproducible.urlRemap} is set and \code{url} is a Drive URL/ID, this
resolves its filename (via the cached \code{assessGoogle()} \code{drive_get()} lookup)
and applies the remap; for plain HTTP(S) URLs it remaps on \code{basename(url)}.
With no remap set, or any failure resolving the Drive filename, the original
\code{url} is returned unchanged.
}
\keyword{internal}
