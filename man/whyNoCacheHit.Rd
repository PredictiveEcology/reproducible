% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/whyNoCacheHit.R
\name{whyNoCacheHit}
\alias{whyNoCacheHit}
\alias{print.whyNoCacheHit}
\alias{print.cacheDryRun}
\title{Why did this call not reuse the cache, and what changed?}
\usage{
whyNoCacheHit(
  cacheId = NULL,
  other = NULL,
  cachePath = getOption("reproducible.cachePath"),
  since = NULL,
  before = NULL,
  n = 3,
  verbose = getOption("reproducible.verbose")
)

\method{print}{whyNoCacheHit}(x, ...)

\method{print}{cacheDryRun}(x, ...)
}
\arguments{
\item{cacheId}{The \code{cacheId} of the run that missed, or the object returned by
\code{Cache(..., dryRun = TRUE)} -- which answers the question \emph{before} the
expensive call runs, since a dry run digests the arguments without
evaluating or saving anything. Defaults to the most recently created entry
in \code{cachePath}, i.e. "whatever my latest run wrote".}

\item{other}{Optional \code{cacheId}(s) to compare against. By default, every other
entry for the same function, ranked so the closest comes first.}

\item{cachePath}{The cache repository. Defaults to
\code{getOption("reproducible.cachePath")}.}

\item{since}{A time (or a \code{difftime}, meaning "ago"). Instead of explaining one
entry, explain a whole run: every entry written after \code{since} is compared
against the closest earlier call of the same function. This is the form to
use when the \code{Cache()} calls belong to modules rather than to you, as in a
\code{SpaDES} pipeline -- ask why the run recomputed, not why one call did.}

\item{before}{With \code{since}, the far end of the window, so a run can be
isolated from whatever else was writing to a shared cache at the time.}

\item{n}{Report at most this many candidate entries (single-entry mode).}

\item{verbose}{Numeric or logical; controls messaging.}

\item{x}{A \code{whyNoCacheHit} object.}

\item{...}{Passed to \code{print.data.frame}.}
}
\value{
A \code{data.frame} of class \code{whyNoCacheHit}, one row per differing
element, with columns \code{candidate} (the entry compared against), \code{created},
\code{element} (the path), \code{status} (\code{"differs"}, \code{"only in this call"},
\code{"only in the other call"}), \code{thisCall} and \code{otherCall} (the hashes).
Candidates that match on every element yield zero rows for that candidate,
which means the two calls digested identically. Printing it gives the
one-line answer.
}
\description{
\code{Cache()} reuses a previous result only when every digested input matches. When
it does not, the question is always which input changed -- and the answer is
recorded: each cache entry carries the hash of every element it digested,
addressed by its path into the call's arguments. This compares one entry
against the other entries for the same function and reports exactly the
elements that differ.

Use it after the fact, on the entry the surprising run wrote. It reads the
repository only, so it needs neither the objects nor a re-run, and it works on
entries written before you suspected anything was wrong.

It is the post-hoc counterpart of \code{options(reproducible.showSimilar = TRUE)},
which reports at call time and stops descending at
\code{reproducible.showSimilarDepth}. This descends as far as the recorded paths
go, so a difference nested inside a parameter list is named in full rather
than reported as "possible, unknown, differences in a nested list".
}
\section{Cost on a real repository}{

Explaining a whole run reads the repository once (about a minute for ~37,000
entries). \code{showCache()} memoises that read per \code{cachePath} for the session, so
later calls are quick; \code{\link[=prepopulateCacheAsync]{prepopulateCacheAsync()}} warms it in a background fork
if you would rather not wait for the first one.
}

\section{How fine the answer is}{

Each entry records its elements to the depth given by
\code{reproducible.showSimilarDepth} (default 3) at the time it was written. A
difference deeper than that is reported at the deepest recorded path, so raise
the option if an answer stops short of the element you need.
}

\examples{
cachePath <- file.path(tempdir(), "whyNoCacheHitExample")
f <- function(x, settings) paste(x, length(settings))
Cache(f, x = 1, settings = list(a = 1, b = list(name = "12.4")), cachePath = cachePath)
Cache(f, x = 1, settings = list(a = 1, b = list(name = 12.4)), cachePath = cachePath)
## Names the element that changed: settings.b.name
whyNoCacheHit(cachePath = cachePath)
}
\seealso{
\code{\link[=showCache]{showCache()}}, \code{\link[=clearCache]{clearCache()}}. \code{Cache(dryRun = TRUE)} asks the same
question before running anything; \code{options(reproducible.showSimilar = TRUE)}
reports at call time.
}
