% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{padYears}
\alias{padYears}
\title{Pad a pair of start and end years to a common width}
\usage{
padYears(years)
}
\arguments{
\item{years}{numeric of length 2, giving the start and end years.}
}
\value{
A list with elements \code{padL} (the common width), and \code{start} and
\code{end}, the two years as padded character strings.
}
\description{
A convenience wrapper around \code{\link[=paddedFloatToChar]{paddedFloatToChar()}} for the common case of a
two-element \code{c(start, end)} vector of simulation years, where both elements
must be padded to the \emph{same} width so that the resulting filenames sort
correctly. That width is taken from the end year, i.e. the larger of the two.
}
\examples{
padYears(c(2011, 2100))
padYears(c(1, 100)) # both padded to 3 characters
}
\author{
Eliot McIntire
}
