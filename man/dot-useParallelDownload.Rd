% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{.useParallelDownload}
\alias{.useParallelDownload}
\title{Decide whether to use the parallel ranged download path}
\usage{
.useParallelDownload(
  url,
  streams = getOption("reproducible.parallel.streams", 48L),
  threshold = getOption("reproducible.parallel.threshold", 10 * 1024^2),
  verbose = getOption("reproducible.verbose", 1)
)
}
\arguments{
\item{url}{The resolved URL.}

\item{streams}{Number of parallel streams (\code{reproducible.parallel.streams}).}

\item{threshold}{Size threshold in bytes.}

\item{verbose}{Numeric verbosity level.}
}
\value{
A list with \code{use} (logical) and \code{info} (the \code{\link[=.probeRange]{.probeRange()}} result, or
\code{NULL} when the probe was skipped).
}
\description{
Internal policy helper for Feature A, separated from the download mechanism so
it can be unit-tested without network access. The parallel path is \strong{gated on
the opt-in \code{reproducible.urlRemap} being set}: if no remap function is set,
it is never used, regardless of \code{reproducible.parallel.streams}. When a remap
is set, the path is used only when \code{reproducible.parallel.streams > 1} (the
default is \code{48L}; set it to \code{1L} to disable), the \pkg{curl} and \pkg{httr2}
packages are available, the server advertises \code{Accept-Ranges: bytes}, and the
file is larger than \code{reproducible.parallel.threshold}. When that gate is not
met, the (potentially network-touching) \code{\link[=.probeRange]{.probeRange()}} call is skipped
entirely.
}
\keyword{internal}
