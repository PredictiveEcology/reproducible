% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloud.R
\name{showCacheCloud}
\alias{showCacheCloud}
\title{List and read all cloud cache metadata (dbFile) files}
\usage{
showCacheCloud(
  cloudFolderID,
  cachePath,
  existingCacheIds = character(),
  drv = getDrv(getOption("reproducible.drv", NULL)),
  conn = getOption("reproducible.conn", NULL),
  verbose = getOption("reproducible.verbose")
)
}
\arguments{
\item{cloudFolderID}{A googledrive dribble of a folder, e.g., using \code{drive_mkdir()}.
If left as \code{NULL}, the function will create a cloud folder with name from last
two folder levels of the \code{cachePath} path, :
\code{paste0(basename(dirname(cachePath)), "_", basename(cachePath))}.
This \code{cloudFolderID} will be added to \code{options("reproducible.cloudFolderID")},
but this will not persist across sessions. If this is a character string, it will
treat this as a folder name to create or use on GoogleDrive.}

\item{cachePath}{A repository used for storing cached objects.
This is optional if \code{Cache} is used inside a SpaDES module.}

\item{existingCacheIds}{Character vector of \code{cacheId}s already known locally;
their cloud metadata is skipped.}

\item{drv}{If using a database backend, \code{drv} must be an object that
inherits from \code{DBIDriver} (e.g., \code{RSQLite::SQLite}).}

\item{conn}{an optional \code{DBIConnection} object, as returned by \code{dbConnect()}.}

\item{verbose}{Numeric, -1 silent (where possible), 0 being very quiet,
1 showing more messaging, 2 being more messaging, etc.
Default is 1. Above 3 will output much more information about the internals of
Caching, which may help diagnose Caching challenges. Can set globally with an
option, e.g., \verb{options('reproducible.verbose' = 0) to reduce to minimal}}
}
\value{
A cache \code{data.table} (possibly empty) of cloud-only metadata.
}
\description{
Downloads every per-\code{cacheId} metadata file (the small \verb{.dbFile.*} files) from
the cloud cache folder, loads them, and \code{rbindlist}s them into a single cache
\code{data.table} with the same shape as \code{\link[=showCache]{showCache()}}. This is what allows
\code{showSimilar} to compare the current call against artifacts cached by \emph{other}
machines sharing the same \code{cloudFolderID}, not just those cached locally.
}
\details{
The full object files are never fetched -- only the small metadata files --
and any \code{cacheId} already present in \code{existingCacheIds} is skipped, so
locally-known artifacts do not pay for a re-download.
}
\keyword{internal}
