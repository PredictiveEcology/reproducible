% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urlLog.R
\name{revalidateRemotes}
\alias{revalidateRemotes}
\title{Re-check downloaded files against their remote sources}
\usage{
revalidateRemotes(
  path = ".",
  recursive = TRUE,
  dryRun = FALSE,
  verbose = getOption("reproducible.verbose")
)
}
\arguments{
\item{path}{Directory to walk. Typically \code{inputPath(sim)} or
\code{outputPath(sim)}.}

\item{recursive}{Logical; recurse into subdirectories. Default \code{TRUE}.}

\item{dryRun}{Logical; if \code{TRUE}, report what would happen without removing
any sidecar. Default \code{FALSE}.}

\item{verbose}{Numeric verbosity.}
}
\value{
Invisibly, a \code{data.frame} with one row per sidecar: \code{file}, \code{url},
\code{status} (one of \code{"unchanged"}, \code{"changed"}, \code{"unreachable"}, \code{"noURL"})
and \code{sidecar}.
}
\description{
Walks the \code{.hash} sidecars written by \code{\link[=preProcess]{preProcess()}} under \code{path}, asks each
recorded URL whether the file has changed, and removes the sidecars of those
that have. Nothing is downloaded here: the next ordinary
\code{\link[=preProcess]{preProcess()}}/\code{\link[=prepInputs]{prepInputs()}} call re-fetches exactly the files whose
sidecar was removed, and leaves the rest alone.
}
\details{
This exists so that re-checking is something a user \emph{does}, occasionally and
deliberately, rather than a mode that gets switched on and forgotten.
\code{options(reproducible.checkRemoteHash = TRUE)} is sticky — set once in a
project's setup and every run pays for it forever — whereas this is a single
action with no lasting state.

How each file is checked depends on what its sidecar holds:
an ETag is revalidated with a conditional \code{If-None-Match} request (one
round-trip, no download); otherwise the recorded digest is compared with the
hash the server currently advertises. Files whose remote cannot be reached
are reported as \code{"unreachable"} and left untouched.

Sidecars written before the URL was recorded have nothing to check against
and are reported as \code{"noURL"}.
}
\examples{
\donttest{
# after a run, ask which inputs have changed upstream
revalidateRemotes(tempdir(), dryRun = TRUE)
}
}
