% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{.urlRemapFn}
\alias{.urlRemapFn}
\title{Resolve the \code{reproducible.urlRemap} option to a remap function}
\usage{
.urlRemapFn(opt = getOption("reproducible.urlRemap"))
}
\arguments{
\item{opt}{The current value of the \code{reproducible.urlRemap} option.}
}
\value{
A \verb{function(url, filename)} or \code{NULL}.
}
\description{
Internal. The \code{reproducible.urlRemap} option may be set to any of:
\itemize{
\item \code{NULL} -- no remapping;
\item a \verb{function(url, filename)} (e.g. built with \code{\link[=makeUrlRemap]{makeUrlRemap()}});
\item a \code{data.frame}/\code{data.table} manifest with \code{filename} and \code{url} columns; or
\item a length-one character path or URL to a CSV with those columns.
This returns a single \verb{function(url, filename)} (or \code{NULL}). For the
\code{data.frame}/character forms it builds the function once via \code{\link[=makeUrlRemap]{makeUrlRemap()}}
and caches it (keyed on the option value), so the lookup is not rebuilt -- and
a CSV path/URL is not re-read -- on every download. An invalid option value is
ignored with a warning (returns \code{NULL}), so it can never break a download.
}
}
\keyword{internal}
