% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/showCacheEtc.R
\name{prepopulateCacheAsync}
\alias{prepopulateCacheAsync}
\title{Pre-populate the in-memory \code{showCache} cache for a given \code{cachePath}}
\usage{
prepopulateCacheAsync(cachePath = getOption("reproducible.cachePath"))
}
\arguments{
\item{cachePath}{A character path. Defaults to
\code{getOption("reproducible.cachePath")}.}
}
\value{
Invisibly returns the spawn job handle, or \code{NULL} if the spawn
was skipped.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Forks a background process that runs \code{showCache()} against \code{cachePath};
subsequent \code{showCache()} / \code{Cache()->showSimilar()} calls in the same R
session can then harvest the result instead of re-scanning the cache
directory synchronously. Useful for very large caches (tens of thousands
of entries) where the cold first scan can take a minute or more.

Idempotent: a second call with the same \code{cachePath} reuses the existing
job. Skipped silently on Windows (forking-based) and when the \code{parallel}
package isn't available.

This helper is called automatically the first time \code{Cache()} or
\code{showCache()} is invoked against a given \code{cachePath}, so most users do
not need to call it explicitly. It is exported for workflows that want
to kick off the spawn early (e.g. inside \code{setupProject()}) so the fork
has more wall-clock time to complete before the first manual
\code{showCache()} call.
}
\examples{
\dontshow{if (identical(Sys.getenv("NOT_CRAN"), "true")) withAutoprint(\{ # examplesIf}
# Scanning a large flat-file cache is slow, so this warms it in the
# background; a later showCache()/Cache(showSimilar = TRUE) then returns
# without waiting. Guarded on interactive() because it spawns a process.
cachePath <- file.path(tempdir(), "prepopulateExample")
dir.create(cachePath, showWarnings = FALSE)

prepopulateCacheAsync(cachePath = cachePath)

unlink(cachePath, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
