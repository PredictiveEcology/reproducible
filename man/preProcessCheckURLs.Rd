% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urlLog.R
\name{preProcessCheckURLs}
\alias{preProcessCheckURLs}
\title{Check downloaded files against their remote sources}
\usage{
preProcessCheckURLs(
  path = ".",
  redownload = c("immediate", "nextPreProcess", "no"),
  recursive = TRUE,
  verbose = getOption("reproducible.verbose")
)
}
\arguments{
\item{path}{Directory to walk. Typically \code{inputPath(sim)} or
\code{outputPath(sim)}.}

\item{redownload}{What to do with files whose remote has changed.
\describe{
\item{\code{"immediate"}}{fetch them now.}
\item{\code{"nextPreProcess"}}{remove their sidecars, so the next ordinary
\code{\link[=preProcess]{preProcess()}} call fetches them. Nothing is downloaded here.}
\item{\code{"no"}}{report only; change nothing.}
}}

\item{recursive}{Logical; recurse into subdirectories. Default \code{TRUE}.}

\item{verbose}{Numeric verbosity.}
}
\value{
Invisibly, a \code{data.frame} with one row per sidecar: \code{file}, \code{url},
\code{status} (\code{"unchanged"}, \code{"changed"}, \code{"unreachable"} or \code{"noURL"}),
\code{action} (\code{"none"}, \code{"sidecarRemoved"}, \code{"redownloaded"} or
\code{"redownloadFailed"}) and \code{sidecar}.
}
\description{
Walks the \code{.hash} sidecars written by \code{\link[=preProcess]{preProcess()}} under \code{path}, asks each
recorded URL whether the file has changed, and reports what it found. The
check always happens; \code{redownload} decides what to do about anything that
has changed.
}
\details{
This exists so that re-checking is something a user \emph{does}, occasionally and
deliberately, rather than a mode that gets switched on and forgotten.
\code{options(reproducible.checkRemoteHash = TRUE)} is sticky — set once in a
project's setup and every run pays for it forever — whereas this is a single
action with no lasting state.

How each file is checked depends on what its sidecar holds: an ETag is
revalidated with a conditional \code{If-None-Match} request (one round-trip, no
download); otherwise the recorded digest is compared with the hash the server
currently advertises. Files whose remote cannot be reached are reported as
\code{"unreachable"} and left alone, as are sidecars written before the URL was
recorded (\code{"noURL"}).
}
\examples{
\donttest{
# what has changed upstream, without touching anything
preProcessCheckURLs(tempdir(), redownload = "no")
}
}
