% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{.parallelRangedDownload}
\alias{.parallelRangedDownload}
\title{Download a file in parallel using HTTP Range requests}
\usage{
.parallelRangedDownload(
  url,
  destFile,
  size,
  n,
  verbose = getOption("reproducible.verbose", 1)
)
}
\arguments{
\item{url}{The (already-resolved, range-capable) URL.}

\item{destFile}{The destination file path to assemble into.}

\item{size}{Total expected size in bytes (from \code{\link[=.probeRange]{.probeRange()}}).}

\item{n}{Number of parallel streams (\code{reproducible.parallel.streams}).}

\item{verbose}{Numeric verbosity level.}
}
\value{
\code{TRUE} on verified success, otherwise \code{FALSE}.
}
\description{
Internal helper for the opt-in parallel download path (Feature A). Splits
\verb{[0, size)} into \code{n} contiguous byte ranges and fetches them concurrently
with one \code{curl} handle per part (each carrying its own \verb{Range:} header) via
\code{curl}'s multi interface. On success the parts are concatenated \strong{in order}
and the total size is verified to equal \code{size}, so the result is
byte-identical to a single-stream download. Returns \code{FALSE} (rather than
erroring) on any partial failure or size mismatch, so the caller can fall
back to single-stream.
}
\keyword{internal}
