% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache-helpers.R
\name{.dumpPreDigest}
\alias{.dumpPreDigest}
\title{Dump the element-by-element \code{preDigest} of a \code{Cache()} call}
\usage{
.dumpPreDigest(
  detailed_key,
  .functionName,
  dump = getOption("reproducible.preDigestDump", NULL),
  pattern = getOption("reproducible.preDigestDumpPattern", NULL),
  verbose = getOption("reproducible.verbose", 1)
)
}
\arguments{
\item{detailed_key}{The list returned by \code{\link[=CacheDigest]{CacheDigest()}} (has \code{key}/\code{outputHash}
and \code{preDigest}).}

\item{.functionName}{The function name for this \code{Cache()} call.}

\item{dump, pattern, verbose}{Resolved from the options above.}
}
\value{
\code{invisible(NULL)}, called for its side effect.
}
\description{
Diagnostic for "why is my \code{cacheId} different here than there?" -- e.g. why a
cloud cache is not shared across machines/OSs. Unlike \code{showSimilar} (which only
reports the \emph{closest} prior call's differences) or \code{dryRun}/\code{verbose}, this
prints the \strong{full} \code{name = hash} list that produced the \code{cacheId}, for \emph{every}
\code{Cache()} call (including ones constructed deep inside other packages, e.g.
SpaDES.core events), so two machines' dumps can be diffed directly.
}
\details{
Controlled by options (off by default):
\describe{
\item{\code{reproducible.preDigestDump}}{\code{NULL}/\code{FALSE} (off); \code{TRUE} to emit each
call's sorted \code{name = hash} list via \code{\link[=messageCache]{messageCache()}}; or a directory path
to write one \verb{preDigest_<functionName>[_<n>].txt} file per call (point it at
a fresh/empty directory, then diff the directory against the other
machine's).}
\item{\code{reproducible.preDigestDumpPattern}}{optional regular expression matched
against the call's \code{.functionName}; only matching calls are dumped (e.g.
\code{"init|inputObjects"}).}
}
}
\keyword{internal}
